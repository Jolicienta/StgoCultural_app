<%= render 'shared/carousel' %>
<div class="container">
  <div class="text-center">
    <h1><strong>En Cartelera</strong></h1>
    <h5>PRÓXIMOS EVENTOS</h5>
  </div>

  <div class="row justify-content-md-center">
    <div class="col-md-10">
      <div class="row">
        <% @events.each do |event| %>
          <div class="col-xs-12 col-sm-6  col-md-6 col-lg-4" style="margin-bottom: 1em;">
            <div class="card">
              <img src="<%= event.pic %>" class="card-img-top" alt="...">
              <div class="card-body">
                <h5 class="card-title"><%= event.title %></h5>
                <%= link_to 'Ir al Evento', event_path(event), class:'btn btn-primary' %>
              </div>
            </div>
          </div>
        <% end %>
      </div>

      <%= link_to 'New Event', new_event_path %>

    </div>
  </div>

  <br>

</div>
